# Core

spring:
  # The name of the application
  application.name: InventoryApplicationTest
  inventoryclient.name: mockclient

inventoryclient:
  name: mockclient

kafka:
  brokerlist: broker1:9092,broker2:9092
